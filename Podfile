platform :ios, '10.0'
use_frameworks!

target 'Demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Demo
  pod 'MBProgressHUD', '~> 1.1.0'
  pod 'EXTView', '~> 0.1.0'
  pod 'SimpleKeychain', '0.9.0'
  pod 'Kingfisher', '~> 4.10.1'

end
